{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the tray popover",
  "windows": ["main", "tray-popover"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
